# 회고록 — DIY_w.AI (간편장부)

## 1. 무엇을 만들었나

개인사업자가 종합소득세 신고용 **간편장부를 스스로 작성**하도록 돕는 웹 서비스.

홈택스에서 받은 전자세금계산서 목록 파일을 올리면, 국세청 간편장부 서식 8개 항목에
자동으로 옮겨 담고, 기준 연도가 아닌 줄을 골라낸 뒤, 사업장별로 저장하고
다시 꺼내 볼 수 있다.

- 저장소 — https://github.com/ctaleez51-art/DIY_w.AI
- 실행 주소 — https://ctaleez51-art.github.io/DIY_w.AI/
- 쓴 것 — HTML/CSS/JS (프레임워크 없음), Supabase(로그인·DB·RLS), SheetJS, GitHub Pages

## 2. 왜 만들었나 — 내 전문 분야의 이 불편을

사업자가 간편장부를 쓸 때, **건건이 직접 입력하거나(수동의 품)
스크래핑에 맡겨 틀린 값을 받는다(자동의 부정확).**
그래서 스스로 신고하려던 사람이 중간에 포기한다.

이 서비스는 그 사이를 간다. **사용자가 가진 파일을 그대로 읽어서 서식에 맞춰준다.**
자료를 만들어내지 않으니 틀릴 여지가 적고, 건건이 칠 일도 없다.

## 3. 결정한 것

무엇을 만들지보다 **무엇을 정하고 넘어갔는지**가 이번 작업의 대부분이었다.

| 결정 | 왜 |
|---|---|
| 매출·매입을 **파일명이 아니라 등록번호로** 가린다 | 파일 안의 `공급자`·`공급받는자` 등록번호를 내 사업장 번호와 맞춰본다. 파일명을 바꿔 올려도 틀리지 않는다 |
| 연도 거르기를 **앱이 알아서 하지 않는다** | 장부 제목에 귀속연도(2026년)를 적어두고, 그 해가 아닌 줄은 색깔로 표시만 한다. 체크는 켜두지 않는다. 빼는 것은 사용자가 직접 골라 `삭제` 를 누르고 `확인` 까지 해야 일어난다. 장부에서 줄이 소리 없이 사라지면 안 된다 |
| 매핑 결과를 **먼저 다 보여주고 나서** 거른다 | 거른 뒤에 보여주면 무엇이 빠졌는지 알 수 없다 |
| 원본 표는 화면에 **띄우지 않는다** | 올린 사람이 자기 파일을 다시 볼 이유가 없다. 올리면 바로 간편장부가 나와야 한다 |
| 비고란에 **증빙종류**(`세계`)를 적는다 | 국세청 간편장부 샘플이 그렇게 돼 있다. 파일 안의 비고 열은 읽지 않는다 |
| 사업장을 **먼저** 만들고 장부를 붙인다 | 장부는 사업장에 딸린 것이다. 붙일 자리 없이 매핑부터 만들면 나중에 다시 뜯어야 한다 |
| `장부보기` 는 **저장된 것만** 보여준다 | 올리기만 한 것도 보여줬더니 저장을 누를 이유가 없어졌다. **보인다 = 저장되었다** 로 맞췄다 |
| `저장` 은 덧붙이지 않고 **화면과 똑같이 맞춘다** | 덧붙이면 저장을 누를 때마다 줄이 불어난다. 뺀 줄은 저장에서도 빠져야 한다 |

## 4. 참은 것

넣고 싶었지만 이번 버전에서 **일부러 안 만든 것**이다. 못 한 것과 다르다.

| 참은 것 | 왜 |
|---|---|
| **계정과목 자동 분류** | 판단이 필요한 영역이다. 규칙을 대충 만들면 틀린 장부가 된다. 칸만 비워뒀다 |
| **카드 이용내역 읽기** | 시간이 모자랐다. 형식은 파악해뒀고 나중에 붙인다 |
| **은행 입출금 내역** | 한 줄이 무엇인지 사람이 판단해야 한다. 이번 범위 밖이다 |
| **품목별로 쪼갠 세금계산서 합치기** | 승인번호가 같으면 한 건으로 봐야 할 것 같은데, 실제로 어떻게 나오는지 확인을 못 했다. 확인하기 전에는 손대지 않았다 |
| **저장한 장부의 수정·삭제** | 장부를 사후에 고칠 수 있게 만드는 것은 별개 문제다 |
| **거래처 사업자번호 같은 추가 정보** | 법정 서식에 없는 칸이다. 다음 버전에서 열을 늘린다 |

## 5. 막혔던 것과 푼 방법

- **표가 몇 행부터 시작하는지 파일마다 다르다** — 세금계산서 목록은 6행부터다.
  줄 번호를 박아두지 않고 **`작성일자` 라는 글자를 찾아** 머리글을 잡게 했다
- **열 이름이 두 번씩 나온다** — `상호`·`주소`가 공급자 쪽과 공급받는자 쪽에 하나씩.
  이름으로만 찾으면 엉뚱한 값을 집는다. **등록번호 열 뒤에서** 찾게 했다
- **고쳤는데 화면이 안 바뀌었다** — 브라우저가 예전 CSS를 들고 있었다.
  서버 파일은 새것인데 화면만 옛날이었다. 주소에 번호를 붙여 새로 받게 했다
- **날짜가 두 가지 모양으로 들어 있었다** — 원래 줄은 글자, 나중에 넣은 줄은 엑셀 날짜값.
  둘 다 같은 모양으로 맞춰서 정렬이 되게 했다
- **삭제 확인창이 이 브라우저에서 뜨지 않았다** — `confirm()` 이 막혀서 삭제가 조용히 취소됐다.
  버튼을 두 번 눌러 확인받는 방식으로 바꿨다

## 6. AI와 어떻게 일했나

- **기술적인 것은 맡기고, 내용은 전부 직접 정했다.** 화면 문구, 항목 이름, 매핑 규칙,
  무엇을 거르고 무엇을 남길지는 하나하나 답을 주고 넘어갔다
- 그래서 **"일단 이렇게 해두고 나중에 고치죠"를 금지**했다. 그게 쌓이면 내가 만든 게 아니게 된다
- AI가 확인하지 않은 것을 아는 것처럼 말할 때가 있었다. 되물으면 뒤집었다.

## 7. 다음에 할 것

- 카드 이용내역, 계산서(면세) 목록, 은행 입출금 내역 붙이기
- 품목별로 쪼갠 세금계산서를 승인번호로 묶기
- 코드값으로 저장하기 — 기장의무·신고유형·업종코드·관할세무서를 고르거나 검색해서 넣게
- 계정과목 분류 — 이 서비스의 본체다. 판단이 들어가는 부분이라 제일 나중에 둔다
