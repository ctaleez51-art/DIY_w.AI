# 정한 규칙

만들면서 정한 처리 규칙을 모아둔다. PRD는 무엇을 만들지, 이 파일은 어떻게 처리할지를 적는다.

## 연도

- 기준 연도는 2026년이다
- 연도가 다른 줄은 제외하고, 몇 건을 제외했는지 사용자에게 알린다
- 판단 기준 날짜 — 카드는 `매출일자`, 세금계산서는 `작성일자`
- 화면에 지금 어느 연도의 장부인지 표시한다

## 파일별 간편장부 칸

- 카드 이용내역 → 비용
- 매출 전자세금계산서 목록 → 수입
- 매입 전자세금계산서 목록 → 비용

## 카드 이용내역 → 간편장부 매핑 (2026-09-13)

| 간편장부 | 파일의 열 |
|---|---|
| ① 일자 | `매출일자` (접수일자가 아니다) |
| ② 계정과목 | 비움 |
| ③ 거래내용 | **비움** — 카드 파일에는 품목에 해당하는 열이 없다. ⚠️ 사용자 확인 필요 |
| ④ 거래처 | `가맹점명` |
| ⑥ 비용 금액 / 부가세 | `매출금액` / `부가세` (카드는 전부 비용) |
| ⑧ 비고 | `카드` |

- 맨 끝의 **합계 줄과 빈 줄은 버린다.** 매출일자가 날짜 모양인 줄만 읽는다
- 날짜가 `2026.01.01` 모양이라 `2026-01-01` 로 바꿔 담는다
- 안 쓰는 열 — 접수일자 · 카드번호 · 승인번호 · 할부개월 · 가맹점번호 · 사업자번호

## 매출인지 매입인지 가리는 법 (2026-09-13)

파일명으로 보지 않는다. **파일 안에 든 등록번호를 고른 사업장의 등록번호와 맞춰본다.**

- `공급자사업자등록번호` 가 내 등록번호 → **매출** (내가 판 것)
- `공급받는자사업자등록번호` 가 내 등록번호 → **매입** (내가 산 것)

## 세금계산서 → 간편장부 매핑 (2026-09-13 사용자가 정함)

| 간편장부 | 파일의 열 |
|---|---|
| ① 일자 | `작성일자` |
| ② 계정과목 | 비움 |
| ③ 거래내용 | `품목명` |
| ④ 거래처 | 매출이면 `공급받는자 상호`, 매입이면 `공급자 상호` |
| ⑤ 수입 금액 / 부가세 | 매출일 때 `공급가액` / `세액` |
| ⑥ 비용 금액 / 부가세 | 매입일 때 `공급가액` / `세액` |
| ⑦ 유형자산 증감 | 비움 (자산인지 비용인지 판단이 필요하다) |
| ⑧ 비고 | `비고` 를 그대로 옮긴다 (품목비고가 아니라 앞쪽 비고) |

⚠️ 상호·대표자명·주소·종사업장번호는 **열 이름이 두 번씩 나온다** (공급자 쪽, 공급받는자 쪽).
이름만으로 찾으면 안 되고, 등록번호 열 뒤에 오는 것으로 가려야 한다.

## 카드 취소 거래

- 취소 거래(음수)와 원래 거래(양수)를 둘 다 장부에 넣는다. 상계하지 않는다

## 세금계산서 금액 — 공급가액인가 품목공급가액인가 (2026-09-13)

- 파일에는 `공급가액`(세금계산서 한 장의 합계)과 `품목공급가액`(품목 한 줄의 금액)이 둘 다 있다
- **지금은 `공급가액` 만 본다** (사용자 지시)
- ⚠️ **나중에 확인하고 다시 정할 것.** 품목을 쪼개서 발행하는 사람이 있다.
  그러면 한 장이 여러 줄로 나뉘는데, 줄마다 `공급가액`(장 전체 합계)이 반복되어 금액이 부풀 수 있다
- 사용자 의견 — **품목별로 나뉘어도 한 번에 발행된 것이라 `승인번호` 는 같을 것이다.
  승인번호가 같으면 세금계산서 1건으로 봐야 한다.** 실제로 어떻게 나오는지는 사용자가 시험해보고 알려주기로 했다

## ⑧비고 — 증빙종류를 쓴다 (2026-09-13 사용자 지시)

간편장부 서식의 비고란에는 **증빙종류**를 적는다. 국세청 간편장부 샘플이 그렇게 돼 있다.

| 증빙 | 비고에 쓰는 말 |
|---|---|
| 세금계산서 | `세계` |
| 계산서 | `계` |
| 카드 | `카드` |
| 현금영수증 | `현영` |
| 현금 | `현금` |

- 이번에 읽는 두 파일(매출·매입 전자세금계산서 목록)은 전부 **`세계`** 다
- 카드 파일을 나중에 붙이면 그 줄은 **`카드`** 가 된다
- **파일 안의 `비고` 열은 읽지 않는다** (2026-09-13 사용자가 정함). 국세청 샘플대로 증빙종류만 적는다

## 계정과목

- 이번 버전에서는 비워둔다. 분류 로직은 나중에 만든다
